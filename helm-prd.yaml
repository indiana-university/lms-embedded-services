
projectedSecret:
  - secret:
      name: "properties-2021-06-07"
  - secret:
      name: "lms-security-properties-20201014"

image:
  tag: latest

env:
  APP_ENV: prd

ingress:
  enabled: true
  hosts:
    - host: "lms-email-service.apps.iu.edu"
      paths: []

activeProfiles: "emailEnabled,emailSigningEnabled,prd"

serviceAccountName: lms-email-impl-prd

cluster: es-prod
