
projectedSecret:
  - secret:
      name: "properties-2021-06-29"

image:
  tag: latest

env:
  APP_ENV: reg

ingress:
  enabled: true
  hosts:
    - host: "lms-email-service-reg.apps-test.iu.edu"
      paths: []