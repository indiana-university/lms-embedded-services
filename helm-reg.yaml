
projectedSecret:
  - secret:
      name: "properties-2021-06-07"

image:
  tag: latest

env:
  APP_ENV: reg

ingress:
  enabled: true
  hosts:
    - host: "lms-email-service-reg.apps-test.iu.edu"
      paths: []

activeProfiles: "emailEnabled,emailSigningEnabled,reg"

serviceAccountName: lms-email-impl-reg

cluster: es-test
