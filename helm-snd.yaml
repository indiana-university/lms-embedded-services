
projectedSecret:
  - secret:
      name: "properties-2021-06-07"

image:
  tag: latest

env:
  APP_ENV: snd

ingress:
  enabled: true
  hosts:
    - host: "lms-email-service-snd.apps-snd.iu.edu"
      paths: []

activeProfiles: "emailEnabled,emailSigningEnabled,snd"

serviceAccountName: lms-email-impl-snd

cluster: es-test
